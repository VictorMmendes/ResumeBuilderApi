<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    @font-face {
      font-family: "Inter";
      font-style: normal;
      font-weight: 100 900;
      font-display: swap;
      src: url("<%= ApplicationHelper.embed_font("Inter-Variable.woff2") %>") format("woff2");
    }

    <%= raw File.read(Rails.root.join('app', 'assets', 'stylesheets', 'pdf.css')) %>
  </style>
</head>
<body>
  <%= yield %>
</body>
</html>
