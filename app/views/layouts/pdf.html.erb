<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    @font-face {
      font-family: "Fredoka";
      font-style: normal;
      font-weight: 300 700;
      font-display: swap;
      src: url("<%= ApplicationHelper.embed_font("Fredoka-Variable.woff2") %>") format("woff2");
    }

    <%= raw File.read(Rails.root.join('app', 'assets', 'stylesheets', 'pdf.css')) %>
  </style>
</head>
<body>
  <%= yield %>
</body>
</html>
